export default async function login(args) {
    return { msg: "Tell the user to click the 'Login' button at the top of the app to sign in. Do not ask them for their username or password in chat — credentials must only be entered in the login form." };
}
