export default async function logout(args) {
    return { msg: "Tell the user to click the 'Logout' button at the top of the app to sign out. Do not log them out through chat — logging out must be confirmed via the Logout button." };
}
